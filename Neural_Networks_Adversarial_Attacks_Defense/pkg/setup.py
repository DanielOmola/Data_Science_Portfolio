from setuptools import setup

setup(
   name='pkg',
   version='1.0',
   description='A useful module',
   author='Daniel Omola',
   author_email='daniel.omola@foo.com',
   packages=['pkg']  #same as name
   
)